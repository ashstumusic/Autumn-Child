# Required — server will not start without this
# Generate with: openssl rand -hex 32
ADMIN_KEY=

# Required for newsletter + automatic show announcement emails
# Get one at https://resend.com (and verify the autumnchild.band domain)
RESEND_API_KEY=

# Optional
PORT=3000
